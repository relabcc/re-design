import React from 'react';

import PatternBase from './PatternBase';

import pattern from './pattern-gray.svg';

const Gray = (props) => (
  <PatternBase src={pattern} {...props} />
);

export default Gray;
