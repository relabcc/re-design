import React from 'react';

import PatternBase from './PatternBase';

import pattern from './pattern-white.svg';

const White = (props) => (
  <PatternBase src={pattern} {...props} />
);

export default White;
